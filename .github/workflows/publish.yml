# Standalone manual fallback for publishing tagged releases to npm.
# The primary publish step now lives in bump-version.yml, so releases
# normally publish end-to-end in a single workflow run. Use this only
# when you need to re-publish or publish a tag created outside the
# Release workflow.
#
# Triggers:
#   - push: tags: v*   (legacy safety net)
#   - workflow_dispatch (manual fallback)

name: Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm
        run: npm install -g npm@latest

      - name: Print toolchain
        run: node -v && npm -v

      - run: npm ci

      - name: Determine dist-tag
        id: dist_tag
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if echo "$VERSION" | grep -q "alpha"; then
            echo "tag=alpha" >> $GITHUB_OUTPUT
          else
            echo "tag=latest" >> $GITHUB_OUTPUT
          fi

      - name: Verify npm token
        run: npm whoami
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Token publish (no --provenance flag). Provenance + token together can 404 if
      # trusted publishing is enabled on npmjs.com. Use trusted publishing instead
      # (remove NODE_AUTH_TOKEN from this step) once configured — see npm docs.
      - name: Publish to npm
        run: npm publish --access public --tag ${{ steps.dist_tag.outputs.tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
